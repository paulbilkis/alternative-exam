function draw_yung_tale (br){
    var x=0,y=0,len=20,n=br.length/2;
    
    ctx.strokeRect(x,y,2*len, n*len);
    for (var i=0; i<br.length; i++){
	if (br[i] == 1){
	    ctx.strokeText(i+1, x+len*i, y);
	}else{
	    ctx.strokeText(i+1, x, y+len*i);
	}
    }
}
